final_emergency_towerlight_node:
  ros__parameters:
    # ===== Serial =====
    port: "/dev/towerlight"   # เช่น "/dev/ttyUSB0"
    baudrate: 9600
    parity: "N"               # "N", "E", "O"
    stopbits: 1
    bytesize: 8
    timeout: 2.0
    slave_id: 1

    # ===== Timers =====
    poll_period: 5.0                 # อ่าน holding registers ทุกกี่วิ
    reconnect_period: 2.0            # ความถี่พยายามเชื่อมต่อใหม่
    turn_green_delay: 0.0            # หลังบูต(ขึ้นไฟแดง) รอกี่วิค่อยเปิดไฟเขียว
    emergency_auto_trigger_delay: 0.0  # 0.0 = ปิด auto-emergency

    # ===== Buzzer Policy =====
    enable_buzzer: false
    buzzer_on_red: false
    buzzer_on_yellow: false
    buzzer_on_green: false
    buzzer_on_emergency: false

    emergency_buzzer_pulse_ms: 0   # 0 = ไม่เปิด buzzer เลยตอนฉุกเฉิน
    force_clear_before_green: true